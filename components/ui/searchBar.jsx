import { View, Text } from 'react-native'
import React from 'react'

const searchBar = ({serachText, data}) => {
  return (
    <View>
      <Text>searchBar</Text>
    </View>
  )
}

export default searchBar